# Clients

Thin index for agents. The dossier (brief, emails, payments, work
dates) lives on the Linear **project**, not here.

Update this file in the same pass as the Linear project.

| Client | GitHub repo | Email | Linear project | Notes |
|---|---|---|---|---|
| | | | | |

`GitHub repo` is `owner/repo`. Use `—` if the site does not exist yet.
`Linear project` is the project URL once created.
